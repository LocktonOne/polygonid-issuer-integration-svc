allOf:
  - $ref: '#/components/schemas/CredentialQrCodeKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - body
          - id
          - typ
          - type
          - thid
          - from
          - to
        properties:
          typ:
            type: string
          type:
            type: string
          thid:
            type: string
          body:
            $ref: '#/components/schemas/CredentialQrCodeBody'
          from:
            type: string
          to:
            type: string